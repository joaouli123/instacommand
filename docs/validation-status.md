# Validation status — 2026-09-21

This is a work-in-progress evidence log, not a claim that the platform is complete.

## Current evidence

- Production ownership transfer completed in one serializable transaction: 20 Instagram links and one Threads link moved from the legacy admin to the confirmed personal workspace. 142 publication records preserved. UI verified two active Instagram accounts, 18 pending choices, and connected Threads.
- Production Analytics inspection: Header displayed @lowfybr while the report displayed @joaouli1. Replaced independent hook state with a shared external store; added protection against stale analytics responses. Four store tests and production build pass. Commit 8c8a1af deployed; live browser verified Header/report alignment and switching from @joaouli1 (5,329 followers) to @lowfybr (3,337).
- Added an independent Threads report endpoint and tab: workspace ownership, period, account metrics, bounded content pagination, CSV export, explicit missing-data/permission/rate-limit states. Threads OAuth now requests threads_manage_insights. Five report tests and six OAuth tests pass. Commit f6d2349 deployed to both services; Coolify rolling updates completed. Live Threads report returned 21 posts over 30 days, second-page navigation worked, and switching to seven days returned two posts. Screenshot inspected. CSV download has not been verified. Per-post Threads insights, audience breakdowns and Facebook reports remain incomplete.
- Production Threads insights diagnostic: both /me/threads_insights?metric=views and the v1.0 equivalent returned HTTP 500 with an empty JSON body. Content reads succeed with the same stored authorization. This does not establish whether the cause is missing scope or a Meta service problem. Never classify this as permission denial without evidence. Existing authorization still needs scope inspection/reconsent validation.
- Reloaded the production Accounts page after transfer: @joaouli1 and @lowfybr remain active, Threads @joaouli1 remains connected, and 18 additional Instagram accounts remain pending user selection. No additional accounts activated, disconnected or publicly posted during this validation.
- Removed name/first-Page guessing from portfolio OAuth discovery. Portfolio-only Instagram assets remain selectable without a fabricated Page ID; verified Page edges take precedence. Facebook publishing now checks the exact Page-to-Instagram relationship before any write, including for legacy records. Backend build and 21 OAuth/link/Threads tests pass, including zero mocked uploads on mismatched mappings. Commit c2e9acc deployed with rolling update complete (lqsmckksusinhbszvwsvooy5).
- Live read-only link checks in the new backend verified @joaouli1 -> Joao Lucas uli and @lowfybr -> Lowfy. No public content was created. Live Facebook fields id/name/followers_count/fan_count succeeded (18/18 and 203/203 respectively). The posts edge returned real older posts with created_time/permalink_url/reactions.summary.total_count/comments.summary.total_count. Shares were absent, not verified zero.
- Separate Facebook report implemented and deployed in 1cc4c16 (backend nd9pnod4wigmsakqnu3bk23s; frontend lvt9xgkakigi48za5klfrjlc; both rolling updates complete). Live UI verified: 30 days for Joao Lucas uli returns zero posts; 730 days returns five, with 18 followers; selecting Lowfy returns three posts and 203 followers. Screenshot inspected. Reports validate ownership and Page identity, bound pagination, preserve partial results, distinguish missing counters from zero, and explain that post counters are lifetime-to-query rather than period-only activity. Seven additional report tests pass, 28 backend tests total; backend/frontend builds and frontend typecheck pass. Reach/views, demographics, graphs and Facebook-only (without Instagram) Page onboarding are not implemented.
- Facebook CSV export code exists with formula neutralization, but live download verification failed: the browser download event timed out after the click, no console error was recorded, and the expected file was absent from the standard Downloads directory. Do not claim export end-to-end verified. No repeated export clicks were made.
- Daily three-post planning added and deployed in e27c5d2 (frontend cn9xbl24hfwubqilxxeqmxsj; backend xzmvxszzwpqdk7ye8r5qpucq; rolling updates completed). New daily mode validates exactly three complete posts, caption limits, formats, times, CTA, creative brief, Story idea and timing caveat. Composer supports editing each caption and explicitly applying an item, with replacement confirmation for existing content. Account changes discard stale requests. Five schema tests and three mocked-provider tests pass; all 33 prior-plus-schema backend tests and frontend/backend builds pass. Production screenshot verified the enabled daily button and existing account selection. Real Gemini generation and applying real results are not verified: an async confirmation asks whether to send @lowfybr bio/metrics/up to eight captions to the configured Gemini. No live generation was submitted. Plans remain in component state and are not yet persisted as drafts; artwork is still a brief, not generated media.
- Daily-plan persistence and draft editing deployed in aaa64f0 (frontend sveozbzn4ofui9ulggvfeeec; backend mmimhtvtqjsbev4oaawls0ll; both show Finished and rolling update completed). This supersedes the earlier volatile-plan limitation: exactly three DRAFT rows can be saved idempotently, reopened in the composer, and edited with media and editorial briefing. Saving never schedules or publishes. Backend readiness guards reject missing media for Instagram/Facebook and incompatible counts/formats. Forty-five backend tests and both builds passed before deployment. Production smoke checks verified the composer loads both Instagram accounts and connected Threads, the calendar finishes loading, and an unknown draft displays an error with a calendar recovery link instead of an editable empty post. That error currently says “Post not found” in English and needs localization. A real generate/save/reopen cycle is NOT yet verified; no real generation, draft creation, scheduling or public publishing was performed in this smoke check.
- Production Accounts rechecked after aaa64f0: @joaouli1 and @lowfybr display Active, Threads @joaouli1 displays connected with a “Conectar outra conta” action. This verifies existing persisted connections, not a new OAuth grant.
- Instagram period/availability correction 2b77cfa: dashboard post interactions, publication table/count, recommendations, format comparison and observed-time ranking now filter the same chosen 7/30/90/365/730-day range. Profile reach/impressions remain explicitly labeled latest-snapshot values, NOT period totals. Post counters are explicitly lifetime-to-collection for publications created in the period. Numeric metric availability is recorded in new additive arrays on profile/post snapshots; unverified legacy zeros are not promoted into verified zeros. API table, timeline and format aggregates retain null for missing values and expose coverage; the UI marks partial interactions and does not draw unknown reach as zero. Recorded zero followers are preserved. Hour ranking now uses one consistent interaction-count unit, excludes completely unknown samples, and warns about small samples. Eight new mocked tests cover bounds/filter propagation/count alignment, verified zero vs legacy ambiguity, partial coverage and Sao Paulo date grouping. Total 59 backend tests, backend build and frontend typecheck/build passed. Production rollout and UI period comparison pending at this entry.
- Screenshot/DOM captured in the browser for the Analytics observation; durable screenshot export has not been completed. This is not a completed visual or accessibility audit.

## Required completion gates

- [ ] Standalone login, logout, session expiry, account isolation, reconnect and multiple-account selection verified end-to-end.
- [ ] Instagram, Facebook and Threads connection status and permissions independently visible.
- [ ] Per-network profile, content, audience and period reports backed by real API data; no substitution between networks.
- [ ] Metrics availability, expired tokens and permission errors accurately explained; zeros distinguished from missing data.
- [ ] Consistent global account selection and race-safe requests verified in production.
- [ ] Upload, preview, order and removal of carousel media; single images, Reels, Stories and text validated.
- [ ] Explicit destination selection and format compatibility for Instagram, Facebook and Threads.
- [ ] Drafts, timezone-aware scheduling, partial failures, retries and duplicate prevention tested. No public test post without approved content.
- [ ] Profile audit and bio/name/positioning recommendations per profile with grounded inputs.
- [ ] Content strategy, feed plan, captions, hashtags and CTAs generated and editable.
- [ ] Artwork creation and carousel art generation implemented and tested, not just text suggestions.
- [ ] Editorial calendar and recommended order/time with sample-size and timezone explanations.
- [ ] Screenshot/insight uploads and analysis supported and tested.
- [ ] Direct-message/comment reply suggestions and supported delivery flows verified separately.
- [ ] Three-post daily plan with captions, CTA, order, times and Story ideas can become editable drafts.
- [ ] Onboarding for nontechnical users; no requirement to provide their own Meta app/token.
- [ ] Loading, empty, failure, mobile and keyboard states tested across all screens; notifications at bottom.
- [ ] Audit remaining trends, competitors, community, notifications, settings and AI configuration for nonfunctional/demo behavior.
- [ ] Tests, commit/push, production deployment and visual/runtime verification completed for each implementation batch.

## Next work

Community b629c98: backend verifies numeric path IDs, workspace/active-account ownership, media association and the comment's own id/media relationship before reply/delete; missing/mismatched/error responses make no remote write. Media fallback also validates returned media ID. Five new mocked tests cover both mutations and fail-closed behavior; 84 backend tests pass and backend build succeeds. Frontend remounts community state by account ID, rejects stale list responses with a request sequence, and suppresses unmounted AI/action updates; account drafts do not transfer. Generic Graph failures no longer become HTTP-200 permission warnings. Frontend typecheck/build and 14 existing tests pass (no automated React race test yet). Backend uzwqu369dfzeqj688hg9z3hw and frontend 6qvwfc3c4rlngnbydu6cnzlj deployments Finished/rolling updates complete; health ok. First comments read during backend replacement failed to fetch, explicit refresh after completion recovered 22 comment fields. Live test entered generic unsent text, switched joaouli1 -> lowfybr -> joaouli1 during loading, observed immediate list/draft isolation and final 22 joaouli1 fields with no stale test text. Draft replies remain volatile and are discarded on account switch; no send/delete occurred. This smoke test is not deterministic network-race proof. Real reply/deletion remains untested. Primary Meta SDK confirms IGComment media field: https://github.com/facebook/facebook-nodejs-business-sdk/blob/main/src/objects/ig-comment.js (retrieved 2026-09-21); developer reference returned 429.

Community inspection: frontend loadComments has no account-generation guard, so an older response can overwrite the newly selected account's comment list; action drafts/loading state also need account scoping. Backend verifies owned account and media but does not verify commentId belongs to that media before reply/delete. Prioritize enforcing this relationship (verify supported Meta fields via primary docs), guard stale requests, distinguish API failures from permission errors, and expose partial collection coverage. Do not test public replies/deletions on real comments without action-time approval.

AI text contracts b23806a: caption, seven-day plan, profile audit and reply now pass explicit bounded Zod schemas instead of returning unchecked provider JSON. Empty/incomplete objects, invalid scores, malformed arrays, excessive captions/hashtags, duplicate plan days, invalid times/formats and duplicate reply alternatives fail explicitly. Unsupported fields are stripped; unknown modes fail closed. Prompts match the new contracts. Six new tests include mocked provider failures for all four modes and prove no hidden paid retry. Backend build and all 79 tests pass. No real provider was called and no social action performed. Real-generation quality and UI handling remain separate verification work; weekly-plan summary/limitations are still not shown by the composer, and audit name/positioning fields and editing/copying need completion. Production backend deploy dbltdjik63c6tqiqo0klwpkm Finished with rolling update complete; health returned ok and unauthenticated generation returned 401. No authenticated production generation was submitted.

AI evidence context ffcae21: generation now selects metric availability/collection dates and normalizes absent or ambiguous legacy zeros to null while retaining verified zeros. Explicit allowlist excludes tokens, workspace IDs and unrelated Page data. Context labels Instagram-only origin, eight-post imported sample, accumulated counters, unknown profile-insight period and snapshot/non-additivity limits; system prompt requires respecting these boundaries. Historical account counters of zero without availability evidence remain unknown. Five new tests cover normalization, dates, absent rows, allowlist and local HTTP generation routing with mocked provider, including workspace/active-account predicate and no provider call when inaccessible. Backend build and all 73 tests pass. No real provider call or user-data transmission was made; generation quality and complete real audit workflow remain unverified. Backend deployment mfhyonr7esvqkzn5nsbprwyo Finished with rolling update completed. Post-deploy health returned ok and unauthenticated POST /api/ai/generate returned 401 without contacting a provider.

Artwork batch c227d3d: added local canvas template editor with light violet/blue/sand themes, editable title/body/signature, 1–10 reorderable pages, 1080x1350 JPEG export into composer media, overflow rejection instead of clipping, and explicit replacement confirmation. This is template-based artwork, NOT AI-generated photography/illustration. Composer can now save a new media-backed draft and reopen it; no automatic scheduling/publishing. Text/layout source is currently component-local, while exported JPEGs can be persisted in the post. Four renderer/wrapping/validation tests plus ten existing frontend tests pass; frontend build passes. Frontend deploy cgoyv7ios7gt6jlnqjyx5wjc Finished. Live test generated two generic JPEGs, created private DRAFT e0ff83a9-783f-448a-b3f1-ada9f2f03cbc labelled TESTE INTERNO — NÃO PUBLICAR, and preserved caption/order/media on reload. Initial remote preview failed, leading to the media fix below. No scheduling or social publication occurred.

Media fix a509144: production API returned HTTP media URLs and Helmet CORP same-origin blocked the separate frontend. Scoped cross-origin embedding to public uploads; new uploads and existing own media URLs use HTTPS when frontend is HTTPS, including worker publishing input. Other origins are not rewritten and API Helmet policy is unchanged. Backend build and 68 tests passed. Coolify backend ipdjwvdhedbprhskldrm6imh Finished with rolling update complete. The same pre-deploy JPEG still returned HTTPS 200 image/jpeg, 88352 bytes, CORP cross-origin after deployment. Reloading the same saved draft visually displayed both pages; clicking next showed page 2/2. The generic test draft remains saved, not published or scheduled. Reels/Stories, media removal/reordering persistence, retries and actual social delivery are not established by this test.

Transport rollout bqee4bfvlv1sjz0gzsmqmnxe finished with rolling update complete. Live Accounts reload retained the session and showed @joaouli1/@lowfybr active and Threads @joaouli1 connected. Synthetic 304/blocked-storage behavior is covered by unit tests, not injected into production traffic.

Frontend transport dad84a8: replaced recursive 304 handling with at most one retry for GET/HEAD, removing conditional validators. POST/PUT/PATCH/DELETE are never automatically retried on 304, 429, 500 or network errors. Headers objects/tuples preserve their values, multipart boundaries remain browser-owned, 204 succeeds without JSON parsing, and blocked localStorage no longer prevents cookie-authenticated reads or a 401 login redirect. Six new transport tests plus four shared-account-store tests pass; frontend typecheck/build pass. This removes one possible request-amplification path but does NOT establish it caused the earlier production rate-limit error. Frontend deployment bqee4bfvlv1sjz0gzsmqmnxe started; rollout pending.

Screenshot feature f411ac7 deployed: backend tufpkoqonqpwrffuhfksnl2c and frontend fdhvygsvmdbhkvjeqhpfloxf both Finished/rolling update completed. Production Accounts DOM/screenshot verified the expandable analysis form, three network choices, objective/audience fields, Gemini model disclosure, unchecked consent and disabled send button without files/consent. Unauthenticated production POST returned 401. No real images were transmitted, no AI quota consumed, and preview/upload/result/copy end-to-end remains unverified.

Screenshot-analysis implementation: authenticated multipart endpoint /api/ai/analyze-images processes at most three PNG/JPEG/WebP files, 4 MB each, in memory; checks signatures/MIME, requires explicit consent, and uses workspace/server Gemini credentials. No account context is automatically added and no public upload URL or ScheduledPost is created. Gemini receives inline image parts with untrusted-image-content boundaries; structured output separates per-image evidence, interpretation, limitations, actions, bio/name suggestions and content ideas. Invalid output or references to absent images fail explicitly. Accounts UI provides previews/removal, objective/audience/network, Gemini disclosure/consent, results and copy. Analysis is not persisted. OpenAI-compatible-only configuration is explicitly unsupported for this feature, not silently sent elsewhere. Seven new mocked service/HTTP-route tests pass (66 total); backend/frontend builds and frontend typecheck pass. Real Gemini image quality/latency/quota and complete live upload/result/copy workflow remain unverified. Reference: https://ai.google.dev/api/generate-content and https://ai.google.dev/gemini-api/docs/generate-content/image-understanding?hl=en (retrieved 2026-09-21).

Pagination follow-up 6c62366 deployed (frontend cnwt2apcsd9bemimm1h2dlne, Finished/rolling update completed). Live re-test after reload: select @joaouli1/24 months -> Content -> Next remains on Content and shows page 2 of 2 with Next disabled and 33 total posts. Overview screenshot also inspected: readable latest-snapshot labels, partial-interaction warning, no fabricated reach chart. This is not yet a full mobile/accessibility audit. New collector availability metadata is covered by helpers/tests; a fresh live Meta collection with the new flags has not been forced or verified.

Instagram batch 2b77cfa deployed successfully: backend drtyikbyqmkqjpkqa9c9x1oq and frontend 5cq93477uedt3ipu5ke9mts1 both completed rolling updates. Live UI: @lowfybr has zero imported publications in both 30/730-day windows; @joaouli1 has 33 posts and 1,280 available interactions at 730 days versus eight posts and 202 at 30 days. Partial coverage and missing reach are explicitly displayed; the content table uses dashes for unknown legacy counters. Pagination exposed an existing UI issue: loading unmounted uncontrolled Tabs and returned to Overview after Next. Follow-up controls the selected tab in page state; typecheck passed, production revalidation pending. No public content created.

Publishing safety batch ed09341: backend build and 51 mocked tests pass. Six new tests cover draft/canceled queue deliveries, future or stale schedule dates, concurrent manual sends, duplicate queue delivery, explicit manual early publishing, and failure of permalink enrichment after a successful remote send. Publishing now claims the row with status/updatedAt compare-and-set before network writes; editing uses the same optimistic condition. This is NOT exactly-once delivery proof: provider timeout ambiguity, process crash after remote success, database persistence failures, queue/database compensation, deletion races and partial-network retries still need a durable per-destination attempt ledger and end-to-end validation. Deployment nb162dw7wrdcyvruorohktgs finished with rolling update completed. Production Accounts reload still shows both Instagram accounts active and Threads connected. No real publication was made.

Verify durable daily-plan-to-drafts editing end-to-end and test real generation when the specific data-sharing choice is answered. Complete report exports and Facebook reach/views/audience coverage; resolve the Threads Insights HTTP 500 with scope inspection/reconsent verification, and finish Threads per-post/audience coverage. The broad AI/editorial/upload/publishing requirements remain open. AI has text modes caption/plan/daily/audit/reply and now a Gemini-only screenshot-analysis mode; a local template artwork editor is available, but AI-generated photography/illustration is still absent. Inspect current official Meta requirements before adding scopes or metric names. Preserve historical data; never guess Facebook Page mappings based on profile names.

Threads reference: Meta-maintained Postman account-insights and post-insights requests, retrieved 2026-09-21: https://www.postman.com/meta/threads/request/4pbwq2u/get-account-insights and https://www.postman.com/meta/threads/request/434u2bd/get-post-insights . Direct developer-documentation retrieval returned HTTP 429; live permission/response verification is still required.
